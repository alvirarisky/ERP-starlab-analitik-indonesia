# ERP Starlab Analitik Indonesia

## Setup
1. Clone repo ini
2. Copy folder `apps/*` ke dalam `frappe-bench/apps/`
3. `bench --site starlab-erp.localhost install-app starlab_lab_ops`

## Dokumentasi
Lihat folder `docs/` untuk BRA, TSD, PRD, dan Project Plan.
